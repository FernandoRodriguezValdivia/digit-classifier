import { CanvasHeight, CanvasWidth, LineWidth, PredictInterval } from "../types/constants.types";

export const CANVAS_WIDTH: CanvasWidth = 280;
export const CANVAS_HEIGHT: CanvasHeight = 280;

export const LINE_WIDTH: LineWidth = 15;
export const PREDICT_INTERVAL: PredictInterval = 500;
